const PI = 3.1416;

const sumar = (a, b) => a + b
const restar = (a, b) => a - b
const multiplicar = (a, b) => a * b
const dividir = (a, b) => a / b

module.exports = {
    PI,
    sumar,
    restar,
    multiplicar,
    dividir
}